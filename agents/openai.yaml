interface:
  display_name: "DraftLedger"
  short_description: "Trace creative state and AI handoffs"
  default_prompt: "Use $draftledger to preserve this project's creative process, govern state, and coordinate auditable handoffs across AI threads."
